# Defines images for each character
# See https://www.renpy.org/doc/html/layeredimage.html#defining-layered-images

layeredimage yuusha:
    group emote auto:
        attribute neutral default
    group body:
        attribute costume default
        attribute hurt

layeredimage ruri:
    group emote auto:
        attribute neutral default
    group body:
        attribute costume default
        attribute hurt

layeredimage satoru:
    group emote auto:
        attribute neutral default
    group body:
        attribute costume default
        attribute hurt

layeredimage mokusei:
    group emote auto:
        attribute neutral default
    group body:
        attribute costume default
        attribute hurt

